package matriz;
import LeitorDeArquivo;
/* DEUS ABENÇOE SUA CLASSE AMEM */
public class Matriz
{
    public double[] Escalona()
    {
        private String arq;
        LeitorDeArquivo leitor = = new LeitorDeArquivo(arq); 
        for(linha = 0; linha<leitor.getDim();linha++)
        {
            int coluna = 0;// coluna sempre será 0, pois é ela que determina o resto do escalonamento
            private dimensoes =  leitor.getDim()
                if(leitor.getMatriz[linha][coluna] > leitor.getMatriz[dimensoes - linha][coluna])
                {
                    if(leitor.getMatriz[linha][coluna] %leitor.getMatriz[dimensoes - linha][coluna])
                    {
                        int divisor = leitor.getMatriz[linha][coluna]/leitor.getMatriz[dimensoes - linha][coluna]);
                        leitor.getMatriz[dimensoes - linha][coluna] = leitor.getMatriz[dimensoes - linha][coluna] - divisor;
                        
                        
                    }
                }
            
        }


    }
    
    
    
  

}